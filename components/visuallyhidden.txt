# DsVisuallyHidden — Cogniterm Design System

import { DsVisuallyHidden } from '~/design'   // Vuetify 불필요 · ds.css만

## 용도
아이콘·시각 정보에 보조기기용 설명을 붙일 때.

## Props
없음

## Slots
- #default — 읽힐 텍스트.

## 사용 예
```vue
<DsVisuallyHidden>검색 결과 12건</DsVisuallyHidden>
```


## 지침
- [해야 할 것] aria-label로 충분하면 그쪽을 먼저.

## 접근성
직접 처리: aria-label로 충분한 경우엔 쓰지 않기

## 시스템 규칙 (요약)
- 색·간격·모서리는 CSS 변수만 (var(--gray-6), var(--brand)) — hex 금지
- primary 버튼은 화면당 하나. 선택·활성 상태는 중립(--sel-bg/--sel-fg)
- 컨트롤 높이 32(sm)/40/48 — 필터 바·툴바는 sm
- 목록·테이블은 default/loading/empty/error/partial 5상태
- 전체 규칙: https://kimjiyong995-coder.github.io/design-system/llms.txt
